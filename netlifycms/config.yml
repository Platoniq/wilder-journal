---
layout: netlifycms/config
permalink: cms/config.yml

# local_backend: true

backend:
  name: git-gateway
  repo: Platoniq/wilder-journal
  # STAGING ONLY — this branch override must NOT reach main.
  # Revert this commit when merging develop into main for go-live.
  branch: develop
i18n:
  default_locale: es
---
